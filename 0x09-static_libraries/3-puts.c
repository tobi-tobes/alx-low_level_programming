#include "main.h"
#include <stdio.h>

/**
 * _puts - prints a string, followed by a new line, to stdout.
 * @str: string to be printed out
 *
 * Return: Always 0.
 */
void _puts(char *str)
{
	puts(str);
}
