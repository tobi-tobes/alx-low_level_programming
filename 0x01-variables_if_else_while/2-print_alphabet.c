#include <stdio.h>

/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	char c;

	/* use for loop and putchar to print alphabet */
	for (c = 'a'; c <= 'z'; c++)
	{
		putchar(c);
	}
	putchar('\n')
	return (0);
}
