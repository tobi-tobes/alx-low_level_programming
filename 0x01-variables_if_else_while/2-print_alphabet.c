#include <stdio.h>

/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	/* use for loop and putchar to print alphabet */
	for (char c = 'a'; c <= 'z'; c++)
	{
		putchar(c);
	}
	return (0);
}
