#include "main.h"

/**
 * mul - multiplies two given digits
 * @a: The first multiplier
 * @b: The second multiplier
 *
 * Return: the product of a and b
 * On error, -1 is returned, and errno is set appropriately.
 */
int mul(int a, int b)
{
	return (a * b);
}
